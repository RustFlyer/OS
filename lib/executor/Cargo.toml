[package]
name = "executor"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config/" }
mutex = { path = "../mutex/" }
arch = { path = "../arch/" }

async-task =  { workspace = true }
lazy_static = { workspace = true }
log = { workspace = true }