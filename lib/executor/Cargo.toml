[package]
name = "executor"
version = "0.1.0"
edition = "2024"

[dependencies]
mutex = { path = "../mutex/" }
async-task =  { workspace = true }
lazy_static = { workspace = true }
log = { workspace = true }