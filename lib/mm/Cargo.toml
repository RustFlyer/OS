[package]
name = "mm"
version = "0.1.0"
edition = "2024"

[dependencies]
buddy_system_allocator = "0.6"
log = "0.4"
config = { path = "../config/" }

[build-dependencies]
config = { path = "../config/" }