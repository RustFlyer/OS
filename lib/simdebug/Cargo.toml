[package]
name = "simdebug"
version = "0.1.0"
edition = "2024"

[dependencies]
arch = { path = "../arch" }
config = { path = "../config" }
log = { workspace = true }
cfg-if = { workspace = true }


[features]
ondebug = []
