[package]
name = "timer"
version = "0.1.0"
edition = "2024"

[dependencies]
arch = { path = "../arch/" }
mutex = { path = "../mutex/" }
simdebug = { path = "../simdebug" }
cfg-if = { workspace = true }
log = { workspace = true }
spin = { workspace = true }